name: Context Freshness

on:
  pull_request:
    paths:
      - 'context/**'
      - 'specs/**'
      - 'docs/decisions/**'
      - 'docs/logs/**'
      - 'scripts/**'
  push:
    branches:
      - main
    paths:
      - 'context/**'
      - 'specs/**'
      - 'docs/decisions/**'
      - 'docs/logs/**'
      - 'scripts/**'
  schedule:
    # GitHub Actions schedules run in UTC.
    # 6:00 AM EST (UTC-5) => 11:00 UTC. Note: this will shift to 7:00 AM ET during DST.
    - cron: '0 11 * * *'
  workflow_dispatch: {}

jobs:
  context-freshness:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Enforce sidecar freshness
        run: node scripts/context-freshness-check.mjs --require-sidecar --fail-on-update --maxAgeDays 14
